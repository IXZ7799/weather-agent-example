[build]
  command = "npm run build"
  publish = "dist"
  node_bundler = "esbuild"

[dev]
  command = "npm run dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
